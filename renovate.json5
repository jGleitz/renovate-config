{
  /* Simulate this configuration locally by running:
     podman run --rm \
         -v "$(pwd):/repo" \
         --workdir /repo \
         --env RENOVATE_TOKEN=$(secret-tool lookup service renovate-dry-run) \
         --env LOG_LEVEL=debug \
         renovate/renovate --dry-run --platform=local
  */
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  extends: ["github>jGleitz/renovate-config:default.json5"],
}
